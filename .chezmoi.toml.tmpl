{{- /* PS interpreter (run .ps1 scripts on Windows). No secrets tracked — fully automated apply. */ -}}
[interpreters.ps1]
    command = "powershell"
    args = ["-NoProfile", "-ExecutionPolicy", "Bypass"]
